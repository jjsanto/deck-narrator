# API Keys (Enter these in the app UI - they are not stored on disk)
# These are just examples - get your actual keys from the respective services

# OpenRouter API Key for AI script generation
# Get yours at: https://openrouter.ai/
# VITE_OPENROUTER_API_KEY=sk-or-...

# Lemonfox TTS API Key for text-to-speech
# Get yours at: https://lemonfox.ai/
# VITE_LEMONFOX_API_KEY=lf-...

# Note: API keys are entered directly in the app UI for security
# They are never stored on disk or sent to any server except the API providers
